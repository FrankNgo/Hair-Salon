@{
  Layout = "_Layout";
}

<h2>Add A New Client for @Model.GetName()</h2>

<form method="post" action="/stylists/@Model.GetId()/clients">
    <div class="form-group">
        <label for="name">First Name</label>
        <input type="text" name="name" class="form-control" required>
    </div>
    <button type="submit" class="btn btn-default">Add Client</button>
</form>

<p class="nav"><a href="/stylists/details/@Model.GetId()">Return to Stylist</a></p>
